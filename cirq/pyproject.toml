[project]
name = "qimono-kernel-cirq"
version = "0.1.0"
description = "Standalone Cirq snippets: Google device exploration and demos"
readme = "README.md"
requires-python = ">=3.11"
dynamic = ["dependencies"]

[tool.uv]
dynamic-dependencies = ["requirements.txt"]
package = false
