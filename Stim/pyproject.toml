[project]
name = "stim-error-correction"
version = "0.1.0"
description = "Error correction experiments with Stim"
requires-python = ">=3.10"
dependencies = [
    "stim",
    "numpy",
    "matplotlib",
]
