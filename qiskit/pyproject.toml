[project]
name = "qimono-kernel-python-3"
version = "0.1.0"
description = "Standalone Qiskit snippets: gates, algorithms, and hybrid workflows"
readme = "README.md"
requires-python = ">=3.11"
dynamic = ["dependencies"]

[tool.uv]
dynamic-dependencies = ["requirements.txt"]
package = false
