[project]
name = "qimono-kernel-pyquil"
version = "0.1.0"
description = "Standalone PyQuil snippets: Rigetti's quantum programming framework"
readme = "README.md"
requires-python = ">=3.11"
dynamic = ["dependencies"]

[tool.uv]
dynamic-dependencies = ["requirements.txt"]
package = false
