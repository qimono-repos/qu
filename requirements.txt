# Root requirements for the qu/ quantum playground.
#
# This file is a convenience for `pip install -r requirements.txt`
# or `uv sync -r requirements.txt`.  Individual workspaces keep
# their own requirements.txt with stack-specific deps.

# --- Shared across all workspaces ---
numpy>=1.26
scipy>=1.11
matplotlib>=3.8
ipykernel>=6.29
jupyter>=1.1
notebook>=7.2

# --- Qiskit (IBM) ---
qiskit>=2.0
qiskit-aer>=0.14
pylatexenc>=2.10
sympy>=1.14.0

# --- Cirq (Google) ---
cirq>=1.4
cirq-google>=1.4

# --- PennyLane (Xanadu) ---
pennylane>=0.45
pennylane-lightning>=0.45
scikit-learn>=1.3

# --- pytket (Quantinuum) ---
pytket>=1.30
pytket-qiskit>=0.60

# --- D-Wave Ocean ---
dwave-ocean-sdk>=6.0
dimod>=0.12
dwave-system>=1.0
dwave-neal>=0.6
networkx>=3.0

# --- Amazon Braket ---
amazon-braket-sdk>=1.80
amazon-braket-local-simulator>=1.0

# --- Bloqade (QuEra / neutral atoms) ---
bloqade>=0.15

# --- PyQuil (Rigetti) ---
pyquil>=4.0

# --- CUDA-Q (NVIDIA) ---
# Uncomment on the GPU box with CUDA toolkit installed:
# cudaq>=0.14
